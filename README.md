# DSP Campaign Router

An internal, access-restricted decision tool. The page contents are
end-to-end encrypted inside `index.html` — open the published link and
enter the team credentials to view it. Ask the AdOps team if you need them.

Nothing sensitive is stored in this repository in readable form.
